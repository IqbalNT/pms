export interface UserInfo {
  _id: string;
  userName: string;
  // name: string;
  // email: string;
  // contact: string;
  // dateOfBirth: string;
  category: string;
  // pictureName: string;
  // status: string;
}
